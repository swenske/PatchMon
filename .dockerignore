# Environment files
**/.env
**/.env.*

# Node modules
**/node_modules

# Logs
**/logs
**/*.log

# Git
**/.git
**/.gitignore
.git-moved

# IDE files
**/.vscode
**/.idea
**/*.swp
**/*.swo

# OS files
**/.DS_Store
**/Thumbs.db

# Build artifacts
**/dist
**/build
**/coverage

# Go binaries and test artifacts (built during Docker build, not from repo)
**/*.exe
!agents-prebuilt/patchmon-agent-*.exe
**/*.test
**/*.out
**/*.prof
**/patchmon-server
**/patchmon-server-*
# Prebuilt server binaries are the image's payload; the rule above would drop them.
!server-prebuilt/patchmon-server-linux-*
# Exclude agent binaries from agents/ (they're copied from agents-prebuilt/ instead)
# but keep agents/ scripts (install, remove, etc.)
agents/patchmon-agent-*

# Temporary files
**/tmp
**/temp

# Docker compose dev data (volumes)
docker/compose_dev_data
**/compose_dev_data
